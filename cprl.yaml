default:
  config:
    aws-profile: default
  services:
    console:
      gov-cloud: true
    codecommit:
      repositories:
        - test-repo1
        - test-repo2
        - other-repo
secondary:
  services:
    codecommit:
      repositories:
        - other-repo
