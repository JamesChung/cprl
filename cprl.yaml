default:
  config:
    aws-profile: poo-poo
  services:
    codecommit:
      repositories:
        - test-repo1
        - test-repo2
        - other-repo
secondary:
  services:
    codecommit:
      repositories:
        - other-repo
